
#include <ciltut.h>

struct bar {
  int red * red r;
  int green g;
  int red green blue c;
};

void foo(int blue c, int r, int g)
{
  return;
}

int main()
{
  struct bar B;
  int red r = 50;

  B.r = &r;
  B.g = 50;
  B.c = AddColor(blue, 50);

  foo(B.c, *B.r, B.g);
  foo(B.g, r, r);

  return 0;
}
